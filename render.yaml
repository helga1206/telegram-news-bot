services:
  - type: worker
    name: telegram-news-bot
    runtime: python3.9
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: NEWS_API_KEY
        sync: false
    plan: free

